spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/shopping_ware?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: ware
  cloud:
    nacos:
      discovery:
        server-addr: 121.41.82.252:8848
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: assign_id
server:
  port: 10001

